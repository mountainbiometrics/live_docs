---
id: "20260616200133"
title: "References render as typed wiki-links"
label: "Typed Wiki-Link Refs"
summary: "Every doc reference rendered to a human is formatted as a typed wiki-link: [Type: Title](id.md). The type is always present so a bare label is never ambiguous, and no separate lookup is needed to know what kind of doc is being referenced. JSON output remains id-keyed for machine consumers."
type: decision
status: living
level: preference
belongs_to: ["[[20260618230225]]"]
requires: ["[[20260615100004]]"]
provenance: ["[[20260615230652]]"]
domain: [presentation]
history:
  - at: "2026-06-16T20:01:33Z"
    summary: "Created doc"
    change_type: [addition]
  - at: "2026-06-18T19:14:57Z"
    summary: "Actualized: show renders edges as [Type: Title](id.md) typed wiki-links; render_body expands [[id]] tokens to typed links at read time"
  - at: "2026-06-18T23:03:20Z"
    summary: "curate-grouping: grouped under index 20260618230225 (Porcelain & CLI)"
  - at: "2026-06-24T20:08:35Z"
    summary: "garden-domains: assigned domains presentation"
  - at: "2026-06-26T20:49:08Z"
    summary: "synthesize-doc-changes: revised — body prose uses bare [[id]]; validate warns on malformed wikilinks and prose-not-edged refs"
  - at: "2026-07-16T22:30:43Z"
    summary: "synthesize-doc-changes: relate typed doc wiki-links to soft term links"
    change_type: [revision]
    session: "20260716222040-8ce7"
  - at: "2026-07-16T22:43:50Z"
    summary: "decouple: restore doc wiki-link decision without term-link coupling"
    change_type: [revision]
    session: "20260716224221-c7a0"
---

## Context

Docs reference other docs via edge fields and body prose. Human-facing porcelain output shows typed wiki-links; body authoring and validate hygiene are separate concerns from that display format.

## Decision

### Human-facing output (porcelain read path)

Any doc reference rendered to a human is formatted as a **typed wiki-link**:

```
[<Type>: <Title>](<id>.md)
```

- `<Type>` is the capitalized doc type (Principle, Decision, Component, Goal, Reference).
- `<Title>` is the full doc title.
- `<id>.md` is the canonical filename, giving a navigable link in any markdown viewer.
- The format is readable raw (type and title are human-legible), carries the id for programmatic navigation, and makes the type always present so a bare label is never ambiguous.

Whether the link text uses the full **Title** or the shorter **label** is left to the label convention (see label-and-title). The current rendering uses Title; a future revision may substitute the label when it is sufficiently descriptive and more compact.

### Body prose authoring (stored markdown)

Authors write doc cross-references in bodies as **bare id wikilinks only**: `[[<id>]]`. The porcelain expands these to typed links at read time.

Malformed body wikilink syntax is invalid authoring:

- `[[<id>|<label>]]` — pipe alias form
- `[[<id>]] (<label>)` — parenthetical label after the link

`validate` warns on malformed forms. Bodies must use bare `[[id]]`.

### Prose vs edge consistency (validate only)

`validate` warns when a body contains a `[[id]]` wikilink whose id is **not present in any edge field** (`requires`, `belongs_to`, `relates`, `provenance`, `superseded_by`). This is report-only — validate does not auto-add edges.

## Consequences

- No reference shown to a human is a bare id or a bare label without type context.
- Agents and humans can immediately tell what kind of doc is being referenced without an extra `ldoc label` call.
- JSON output is unaffected — it remains id-keyed for machine consumers.
- The review-summary ledger already renders entries with `<Type>: <label> — <intent>`; this decision formalizes the pattern for all reference output.
