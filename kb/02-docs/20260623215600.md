---
id: "20260623215600"
title: "Decision: Viewer default doc sort is type rank then label"
label: "Viewer Doc Sort Order"
summary: "Where the viewer lists docs without a surface-specific sort, order is type rank (use-case through reference, high-level first) then alphabetical label; reviews, catalog column picks, dashboard entry-point ranking, and hot-doc history counts keep their established sorts."
type: decision
status: living
level: preference
belongs_to: ["[[20260619204727]]"]
provenance: ["[[20260623215513]]"]
domain: [presentation]
created: "2026-06-23T21:56:00Z"
history:
  - at: "2026-06-24T20:08:42Z"
    summary: "garden-domains: assigned domains presentation"
---

## Decision

Where the viewer lists docs and **no surface-specific sort already applies**, order is **type rank, then label** (alphabetical on `label`, falling back to `title` then `id`).

**Type rank** (high-level → concrete):

`use-case` → `principle` → `requirement` → `constraint` → `component` → `decision` → `guide` → `reference`

Types outside this list sort after `reference`, alphabetically by type name, before the label tiebreak.

## Exceptions — established sorts trump this default

- **Reviews** — newest first (review id / timestamp).
- **Catalog** — user-selected column sort overrides the default; group-by-type headers follow type rank.
- **Dashboard entry-point signposts** — subtree size (descendant count), as an intentional entry-point signal.
- **Hot docs** — history-entry count.

## Surfaces using the default

Hierarchy sibling order, member lists, connection/backlink groups, orphan and deprecated lists, catalog default rows (before a column header is chosen), and the type-icon legend.

## Rationale

Subtree-size ordering at hierarchy roots and ad hoc per-surface sorts were unpredictable. Type-first ordering matches how readers scan for intent; a stable label tiebreak is grep-friendly.
