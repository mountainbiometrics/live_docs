---
id: "20260618214315"
title: "Boxes colocated under kb/ with numbered distillation stages"
label: "Numbered KB Boxes"
summary: "The input boxes live under a single kb/ parent as 00-inbox/, 01-raw/, and 02-docs/, while the reviews ledger is kb/reviews/ — unnumbered, because it is process state rather than a distillation stage. Colocating the boxes declutters the repo root, and the numeric prefixes encode the distillation gradient from frictionless capture to accepted clipping to atomic doc. The layout was chosen per the incidental-patterns principle: the numbered-box convention is borrowed from prior art while the system's own schema and edge decisions are kept intact."
type: decision
status: living
level: preference
requires: ["[[20260618214128]]"]
provenance: ["[[20260618214113]]"]
scope: [live_docs]
created: "2026-06-18T21:43:15Z"
---

## Decision

The input boxes live under a single **`kb/`** parent: `00-inbox/`, `01-raw/`, `02-docs/`. The reviews ledger is **`kb/reviews/`** — **unnumbered**, because it is *process state*, not a distillation stage.

## Rationale

Colocation under `kb/` **declutters the repo root**, and the numeric prefixes encode the **distillation gradient**: capture → accepted clipping → atomic doc. The numbered-box shape is **borrowed from prior art** (the schema-concept-learning-kb), chosen per the incidental-patterns principle — adopt the incidental convention while keeping our own schema and edge decisions intact.
