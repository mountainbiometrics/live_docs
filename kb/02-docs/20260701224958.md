---
id: "20260701224958"
title: "History records creation; the created field is dropped"
label: "Creation Is Recorded History"
summary: "Creation is recorded as a doc's first history entry (an addition), superseding the rule that history excluded creation. The separate created field is dropped — creation time is the addition entry's timestamp — via a one-shot migration, pure back-compat reads, and heal-on-next-write."
type: decision
status: target
level: preference
belongs_to: ["[[20260618230216]]"]
provenance: ["[[20260701224818]]"]
created: "2026-07-01T22:49:58Z"
---

## Decision

A doc's creation is recorded as its first **history entry** — an `addition` (see the change-type taxonomy), stamped with the session that created it — rather than being left implicit. This **supersedes** the earlier rule that history excluded creation.

## Consequences

- The separate `created` frontmatter field is **dropped**: a doc's creation time is the `at` of its addition entry, and the doc id already encodes it.
- Every doc now carries at least one history entry, so `history: []` is no longer a valid resting state.
- The addition entry is **excluded from the hot-file churn signal** — creation is not churn.

## Migration and back-compat

Existing docs are converted by a one-shot `ldoc migrate` (each `created` becomes an addition entry; the field is removed). Reads **tolerate** a lingering `created` — an outdated writer may still emit one — and stay **pure**, never rewriting on read. A doc is healed on its **next write** for any reason.
