---
id: "20260618000209"
title: "apply-to-docs must pause and warn when blast radius is large"
label: "Apply-to-docs Must Pause"
summary: "Before writing any changes, the apply-to-docs skill must evaluate the total blast radius of a request against the store and halt with a structured warning if the impact exceeds defined thresholds: two or more full supersessions, four or more combined partial and full supersessions, or any unresolved conflicts. The warning must list total affected doc count, per-verdict counts, affected doc ids and titles, and a conflict description, and the user must explicitly confirm before writes proceed. This gate exists because silently applying many supersessions could destroy accumulated knowledge that represents a paradigm shift or expose a mistake the user should catch before it propagates."
type: requirement
status: living
level: requirement
requires: ["[[20260618000147]]"]
scope: [live_docs]
created: "2026-06-18T00:02:09Z"
---

## Requirement

Before writing any changes, the `apply-to-docs` skill must evaluate the total blast radius of the request's impact on the store. If the impact exceeds defined thresholds, the skill must halt and surface a structured warning to the user before proceeding.

## Thresholds (any one triggers the gate)

- `supersede-full` count ≥ 2
- `supersede-partial` + `supersede-full` combined ≥ 4
- Any `conflict-unresolved` docs present

## Warning format

The warning must list: total affected doc count, per-verdict counts, a doc-by-doc list with ids and titles, and a conflict description for any `conflict-unresolved` items. The user must explicitly confirm (yes / no / resolve conflicts first) before writes proceed.

## Rationale

A request that overrides many prior decisions may represent a paradigm shift or a mistake. Silently applying many supersessions destroys the accumulated knowledge without giving the user a chance to reconsider or catch errors.
