---
id: "20260617212613"
title: "Frontmatter omits empty list fields; absent means empty"
label: "Omit Empty Lists"
summary: "Empty list-valued frontmatter fields are omitted entirely; absent means empty and is valid. Scalar fields are always written explicitly. The tags block is dropped when both domain and scope are empty. This keeps frontmatter minimal, eliminating the noise of uniform empty placeholders that characterized early docs."
type: decision
status: living
level: preference
belongs_to: ["[[20260618230216]]"]
requires: ["[[20260617212538]]"]
created: "2026-06-17T21:26:13Z"
history:
  - at: "2026-06-17T22:47:58Z"
    summary: "garden consistency: upgraded relates->requires on 20260617212538 (Status Enum Unified) — frontmatter omission rule depends on the schema decision"
  - at: "2026-06-18T23:03:00Z"
    summary: "curate-grouping: grouped under index 20260618230216 (Doc Lifecycle & Schema)"
---

## Context

The frontmatter schema has several list-valued fields: `requires`, `belongs_to`, `relates`, `provenance`, `superseded_by`, `history`, and the `tags` sub-object (`domain`, `scope`). When a doc has no edges of a given type, two representations are possible: write the field as `[]` (explicit empty) or omit the field entirely.

## Decision

Empty list fields are omitted from frontmatter. Absent means empty/default, and absent is valid. Scalar fields (`type`, `status`, `level`, `id`, `title`, `label`, `created`) are always written explicitly. The `tags` block is omitted entirely when both `domain` and `scope` are empty.

## Consequences

- Docs created by `ldoc new` omit any edge list that would be empty.
- The canonical frontmatter form is minimal: only fields with actual content appear.
- Validate does not error on absent list fields; it treats them as empty.
