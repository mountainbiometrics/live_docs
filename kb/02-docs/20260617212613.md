---
id: "20260617212613"
title: "Frontmatter omits empty list fields; absent means empty"
label: "Omit Empty Lists"
summary: "Empty list-valued frontmatter fields are omitted entirely rather than written as empty brackets, so absent means empty and is valid. Scalar fields (type, status, level, id, title, label, created) are always written explicitly, and the tags block is dropped entirely when both domain and scope are empty. This keeps frontmatter minimal and readable, eliminating the noise of uniform empty placeholders that characterized early docs. The ldoc new command and validate both respect this convention, and migrating existing docs with [] entries is treated as a cosmetic bulk pass with no semantic change."
type: decision
status: living
level: preference
requires: ["[[20260617212538]]"]
scope: [live_docs]
created: "2026-06-17T21:26:13Z"
history:
  - at: "2026-06-17T22:47:58Z"
    summary: "garden consistency: upgraded relates->requires on 20260617212538 (Status Enum Unified) — frontmatter omission rule depends on the schema decision"
---

## Context

The frontmatter schema has several list-valued fields: `requires`, `belongs_to`, `relates`, `provenance`, `superseded_by`, `history`, and the `tags` sub-object (`domain`, `scope`). When a doc has no edges of a given type, two representations are possible: write the field as `[]` (explicit empty) or omit the field entirely. Early docs used `[]` uniformly, producing noisy frontmatter where most of the content was empty placeholders.

## Decision

Empty list fields are omitted from frontmatter. Absent means empty/default, and absent is valid. Scalar fields (`type`, `status`, `level`, `id`, `title`, `label`, `created`) are always written explicitly. The `tags` block is omitted entirely when both `domain` and `scope` are empty.

## Consequences

- New docs created by `ldoc new` omit any edge list that would be empty.
- The canonical frontmatter form is minimal: only fields with actual content appear.
- Validate does not error on absent list fields; it treats them as empty.
- Migrating existing docs that have `[]` entries is a cosmetic bulk pass; it carries no semantic change.
