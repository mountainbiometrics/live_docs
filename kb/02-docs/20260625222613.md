---
id: "20260625222613"
title: "live_docs captures the why; code already holds the what"
label: "Capture the Why Not the What"
summary: "live_docs captures the why — decision process, use-cases, constraints, requirements — not the what the code already encodes. The what is recoverable from code; the why is not, and it outlives any implementation. The test for a doc: would it still be true after the code it describes was rewritten?"
type: principle
status: living
level: requirement
belongs_to: ["[[20260618230214]]"]
relates: ["[[20260615100001]]", "[[20260615182358]]", "[[20260617212538]]", "[[20260707164539]]"]
provenance: ["[[20260625221953]]"]
domain: [governance]
keywords: [why, rationale, intent, decision-process, use-cases, constraints]
history:
  - at: "2026-06-25T22:26:13Z"
    summary: "Created doc"
    change_type: [addition]
  - at: "2026-06-25T23:22:37Z"
    summary: "apply-to-docs (follow-up): added the prescriptive direction — docs are the current intended paradigm code aligns to, and a doc need not be implemented to be living (status: target). Captures the goal-of-the-system point rather than spawning a duplicate. Per request 20260625221953."
  - at: "2026-07-07T16:49:36Z"
    summary: "revise-doc: decomposed — extracted the 'docs lead, code aligns' architectural decision into its own doc (20260707164539); this doc now states the why-not-what principle alone."
    change_type: [revision]
    session: "20260707164455-9a78"
---

## Statement

live_docs exists to capture the **why**: the decision process behind a plan, the use-cases it set out to solve, the constraints that bounded the choices, and the requirements it had to satisfy. It does **not** exist to restate the **what** — the structure and behavior the code already encodes.

## Rationale

The *what* is recoverable from the code at any moment; the *why* is not. Plans and design docs embedded in codebases are almost always stale — often before the plan is even finished — precisely because they snapshot implementation detail. The abstract why outlives any particular implementation: switching a library, renaming a module, or re-drawing a boundary leaves the motivating constraint, use-case, and requirement intact.

This extends to the *current state itself*. When something was built and later removed, the artifact and the git history show *that* it changed — but only a why-doc records *why the current state is what it is*. Without it, a future reader sees the residue of a decision with no way to tell whether the current shape is deliberate or accidental.

A discovery, ingest, or reconcile pass that recreates the *what* from the *why* has **failed**: it has merely transcribed the code into prose, producing many implementation-detail docs and almost no durable knowledge. The test for every doc is whether it would still be true and useful after the implementation it describes was rewritten.

The directional consequence — that the docs *lead* and the code aligns to them, not the reverse — is its own architectural decision: [[20260707164539]].
