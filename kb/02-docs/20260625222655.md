---
id: "20260625222655"
title: "Why the decision type is reserved for scoped architectural choices"
label: "Decisions Are Architectural and Scoped"
summary: "Reserve decision for architectural choices among alternatives, scoped via belongs_to. Keep decisions thin: they require first-class why-web roots (principles, constraints, requirements, goals) rather than burying the why only in the decision body or restating outcomes."
type: guide
status: living
level: preference
belongs_to: ["[[20260618230216]]"]
relates: ["[[20260615100015]]", "[[20260618214324]]", "[[20260619235018]]", "[[20260625222613]]", "[[20260707170659]]"]
provenance: ["[[20260625221953]]", "[[20260723223148]]"]
domain: [doc-lifecycle, governance]
history:
  - at: "2026-06-25T22:26:55Z"
    summary: "Created doc"
    change_type: [addition]
  - at: "2026-06-26T00:49:56Z"
    summary: "Re-typed decision -> guide: this is how-to-use guidance, not an architectural decision of the system — it was itself mis-typed as a decision, the exact error it warns against. Operative classification ladder moved to _shared/doc-types.md; this keeps the why. Per request 20260625221953."
  - at: "2026-07-23T22:37:29Z"
    summary: "synthesize-doc-changes: provenance from why-web altitude episode 20260723223148"
    change_type: [revision]
    session: "20260723223120-c5d1"
---

## What this is

A guide to choosing — and not over-using — the `decision` type. The operative
classification ladder (all the types, the "is this really a decision?" test)
lives in `.claude/skills/_shared/doc-types.md`; this records *why* the decision
type is drawn the way it is.

## Why decisions are special

Almost any fact about the system was, at some level, decided — so `decision` is
the type most easily over-applied, and in this store it became the largest and
most misclassified type. Reserving it for **architectural choices among
alternatives** is what keeps the taxonomy meaningful: if everything is a
decision, the type tells a reader nothing.

## What qualifies

A `decision` records a deliberate choice among real alternatives, with a
rationale, that future work shouldn't have to re-decide. Consequences:

- **Architectural, not existential.** It must be restatable as "X over Y because
  Z," and **excisable** — swapping the choice (a serialization format, a
  library) should be a one-atomic-doc change, the doc analogue of good
  architecture. A "decision" that merely asserts a thing *exists* is a
  `component`; one that tells an actor *how to work* is a `guide`.
- **Thin, wired to the why-web.** Prefer first-class principles, constraints,
  requirements, and goals for the driving reasons ([[20260625222613]]); the
  decision should be a thin modeling node that **`requires`** those roots so
  relations can be modeled and re-evaluation has something to consult
  ([[20260707170659]]). Do not invent a pile of outcome-restating docs ("X lives
  in Y") whose bodies bury or omit the why.
- **Scoped to the level it binds.** Place it via `belongs_to` under the subtree
  it governs, so it constrains that subtree and not its siblings. A decision with
  **no** `belongs_to` is **global** — legitimate for genuinely cross-cutting
  architecture (the foundational principles are effectively global), wrong for
  one that should bind only a single project. (See the placement shared
  definition for what the hierarchy means.)
