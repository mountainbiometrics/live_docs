---
id: "20260615100017"
title: "live_docs is the portable system; sinai is MTN's instance"
label: "Livedocs vs Sinai"
summary: "live_docs is a portable, reusable system; sinai is MTN's specific instance. System-specification docs sit under the root scope anchor (live_docs), inheriting it through belongs_to lineage rather than each carrying a hand-stamped scope:[live_docs] tag, so they can be extracted into a standalone repo without MTN-specific knowledge."
type: decision
status: living
level: preference
requires: ["[[20260615100000]]"]
relates: ["[[20260619235018]]"]
provenance: ["[[20260615203646]]"]
scope: [live_docs]
created: "2026-06-15T10:00:17Z"
history:
  - at: "2026-06-19T23:52:27Z"
    summary: "Reframe: scope:[live_docs] is now inherited from the root scope anchor via belongs_to lineage (topological scope), not a per-doc tag; portability intent unchanged. Per reframing 20260619234841."
---

## Context

live_docs is being built inside the sinai repo, which is MTN's specific project. System-level docs (principles, decisions about the doc system itself) need to be separable from MTN-specific domain docs.

## Decision

System-specification docs live **under the root scope anchor** — the `live_docs` scope declared on the root component and **inherited through `belongs_to` lineage** — rather than each doc hand-carrying a `scope: [live_docs]` tag. Their **effective scope** therefore includes `live_docs` by virtue of where they sit in the hierarchy. MTN-specific docs accumulate in their own zone (their own scope anchor), outside the `live_docs` genealogy.

## Consequences

The system stays portable: live_docs can be extracted by lifting the subtree under the `live_docs` scope anchor, and effective scope (a topological computation) tells you cleanly which docs that is. MTN-specific knowledge does not pollute the generic system definition because it sits under a different anchor. The discipline shifts from "stamp the tag on every system doc" to "keep system docs in the live_docs genealogy"; a gardening skill can verify that placement.
