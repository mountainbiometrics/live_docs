---
id: "20260701225016"
title: "Every mutation double-writes a WAL line; the WAL is the review's source of truth"
label: "Session Change Log"
summary: "During a session every mutation writes one WAL line (link/unlink also record per-type edge add/remove counts); the per-doc history is materialized at close as one collapsed entry per touched doc, and the review is built from the same WAL."
type: decision
status: target
level: preference
belongs_to: ["[[20260701224960]]"]
relates: ["[[20260701201617]]", "[[20260710235235]]"]
provenance: ["[[20260701224818]]"]
domain: [governance]
history:
  - at: "2026-07-01T22:50:16Z"
    summary: "Created doc"
    change_type: [addition]
  - at: "2026-07-03T18:27:47Z"
    summary: "WAL-only during the session; per-doc history is a projection materialized at close (history-from-wal), replacing the per-mutation double-write"
    change_type: [revision]
    session: "20260703182747-347d"
  - at: "2026-07-10T23:46:43Z"
    summary: "summary: mention edges_added/removed on link/unlink WAL lines"
    change_type: [revision]
    session: "20260710234642-8a5c"
  - at: "2026-07-10T23:53:13Z"
    summary: "review integration snapshot consumes WAL edge counts"
    change_type: [organizational]
    session: "20260710235131-d115"
---

## Decision

During an open session, every mutation appends **one WAL line** to the session's record — the WAL is the **single in-flight log** of what changed. The per-doc **`history:`** is *not* written per mutation. At **close**, the WAL is collapsed into **one history entry per touched doc** (its dominant change_type, best author note, and last-touch time) written to the docs, and the session's **review** is built from the same WAL. History is a *projection* of the WAL, so the two can never diverge.

## Why the WAL is the in-flight source of truth

Writing a per-doc history entry for every keystroke bloats a doc's trail — one session that creates and then refines a doc five times would leave five entries where one belongs. Keeping the granular log in the WAL and projecting it to a single session-granular history entry at close keeps each doc's trail readable (one entry per session that touched it) while the full detail survives in the review's WAL archive. A **deletion** also needs the WAL: it removes the doc, so it leaves no history — the WAL line records the dead doc's label/type and the edges stripped, so the change stays auditable and **replayable**.

## Entry shape

Each WAL line records the operation, the target ref, the **change_type(s)** it touched (a list — see the change-type taxonomy), and the note (flagged author-supplied or auto). On `link`/`unlink`, the line also carries optional per-type **count** dicts `edges_added` / `edges_removed` (e.g. `{"requires": 1, "relates": 2}`) — counts only, no targets; provenance is never recorded. Those counts feed the review's mint-time `integration.edges_added_to_existing` total. The collapsed history entry carries the single **dominant** change_type. The timestamp-window (`--since`) path is retained as a fallback for building a review without a session.
