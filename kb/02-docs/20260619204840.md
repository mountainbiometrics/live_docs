---
id: "20260619204840"
title: "Decision: Index docs recombine decomposed docs at the presentation layer"
label: "Index Recombination"
summary: "Index docs (type:index) are the recombination mechanism that resolves the decomposition-vs-colocation tension at the presentation layer. Rendering an index = its orientation-guide body plus, on demand, its member docs' bodies inlined as sections, gathered via belongs_to. Docs stay decomposed and single-responsibility on disk; the viewer recombines them into readable wholes at view time."
type: decision
status: living
level: preference
belongs_to: ["[[20260619204727]]"]
requires: ["[[20260615100003]]"]
relates: ["[[20260615204138]]", "[[20260619204822]]"]
provenance: ["[[20260619204653]]"]
scope: [live_docs]
created: "2026-06-19T20:48:40Z"
---

## Context

Single Responsibility pushes docs to be tiny and decomposed on disk. Readers, however, often want a colocated whole — the full picture of a subtree in one place. That is a real tension, and the wrong fix would be to colocate on disk (which would violate decomposition).

## Decision

Resolve the tension at the **presentation layer** using `type:index` docs as the **recombination mechanism**. Rendering an index is:

- its **orientation-guide body** (the synthesis a reader reads to get oriented), plus
- on demand, its **member docs' bodies inlined as sections**, gathered by following `belongs_to`.

Docs stay **decomposed and single-responsibility on disk**; the viewer **recombines** them into a readable whole only at view time, via the same `![[id]]` transclusion path.

## Rationale

This keeps the disk store faithful to decomposition while giving readers the colocated reading experience they want, without duplicating content. The index doc already exists as the navigational signpost and carries an orientation guide; making it the recombination surface is a natural extension — the guide frames the members, and the members fill in the detail on demand.
