---
id: "20260615230716"
title: "ldoc — the live_docs porcelain CLI and KB API"
label: "Ldoc CLI"
summary: "ldoc is the unified porcelain CLI and programmatic KB API for the live_docs store: reads, graph traversal, dumb mutators, review ledger verbs, user config (ldoc config), and maintenance. Every ref accepts id, label, or title substring."
type: component
status: living
level: preference
belongs_to: ["[[20260618230225]]"]
requires: ["[[20260615230652]]", "[[20260615230659]]"]
relates: ["[[20260622213948]]", "[[20260626204912]]", "[[20260702205006]]", "[[20260716212859]]"]
provenance: ["[[20260624225405]]"]
history:
  - at: "2026-06-15T23:07:16Z"
    summary: "Created doc"
    change_type: [addition]
  - at: "2026-06-18T23:03:19Z"
    summary: "curate-grouping: grouped under index 20260618230225 (Porcelain & CLI)"
  - at: "2026-06-22T21:40:01Z"
    summary: "apply-to-docs: documented review ledger and ldoc config user-identity verbs"
  - at: "2026-06-23T19:18:14Z"
    summary: "apply-to-docs: documented --no-viewer flag and automatic viewer rebuild after doc/review mutations"
  - at: "2026-06-23T23:41:21Z"
    summary: "synthesize-doc-changes: revised — documented keywords support: find term-matches keywords (+--keyword filter), new/set accept --keywords (replace semantics), validate treats keywords as free-text trimmed/de-duped. Related to 20260623233935."
  - at: "2026-06-24T19:58:54Z"
    summary: "garden-refine: label cleanup — 'ldoc CLI' → 'Ldoc CLI'"
  - at: "2026-06-24T22:56:15Z"
    summary: "Revised: added rm verb to dumb mutators table"
  - at: "2026-06-26T20:49:04Z"
    summary: "synthesize-doc-changes: revised — rm blocks on inbound edges (--force strips all inbound then deletes); unlink accepts literal dead 14-digit ids; validate warns on staged retirement, prose-not-edged wikilinks, malformed wikilinks; edges/validate share unified dangling detection"
  - at: "2026-07-02T20:50:31Z"
    summary: "apply-to-docs: viewer rebuild targets STORE_ROOT/build/viewer.html"
    change_type: [revision]
    session: "20260702204951-4be0"
  - at: "2026-07-02T21:53:03Z"
    summary: ""
    change_type: [organizational]
    session: "20260702214904-22a4"
  - at: "2026-07-16T21:29:37Z"
    summary: "synthesize-doc-changes: revised — dropped keywords from find/new/set/validate surface; field removed (20260716212859)"
    change_type: [revision]
    session: "20260716182039-0fef"
---

## Capability description

`ldoc` is the unified porcelain over the live_docs markdown store. It ships as two surfaces:

1. **`ldoc`** — shell command (`scripts/ldoc.py`), on `PATH` via install.sh symlink; fallback: `python3 scripts/ldoc.py <subcommand>`.
2. **`KB` class in `scripts/livedocs/`** — importable for skills and scripts that need programmatic access.

Global flag: **`--no-viewer`** — skip the automatic viewer rebuild after mutating commands (see Maintenance).

### Read surface

| Verb | What it returns |
|------|-----------------|
| `get <ref>` | Frontmatter summary for a doc |
| `body <ref>` | Plain body text of a doc |
| `show <ref>` | Full doc with edges resolved to human labels (never bare ids/filenames) |
| `find <query>` | Search/filter docs by type, tag, or text; term match covers title, label, and body. Faceted filters: `--scope`, `--domain`. |
| `ls [--type T]` | List all docs, optionally filtered by type |
| `map` | Entry points (signpost roots) with summaries — orient here |
| `resolve <ref>` | Resolve id, label, or title substring to canonical id |
| `label <ref>` | Print `<Type>: <Title>` label for a ref |

### Graph surface

| Verb | What it does |
|------|--------------|
| `neighbors <ref>` | Show direct edge neighbors by kind |
| `graph <ref>` | BFS traversal over cascade-hard edges from a starting doc |
| `edges` | Print forward and reverse edge maps; reports dangling outbound edges across all blocking edge types (unified with `validate`) |

### Dumb mutators

Mutators carry NO judgment — they apply the requested change and nothing else. Skills (`revise-doc`, `ingest-reference`, `cascade-check`, `garden`) decide when to cascade, add history, or dedup.

| Verb | What it does |
|------|--------------|
| `new` | Create a new doc with frontmatter and optional body; accepts `--tags-domain` / `--tags-scope` among other fields |
| `set <ref> --field value` | Update scalar frontmatter fields on an existing doc (`--title`, `--label`, `--summary`, `--level`, `--status`, `--type`, `--scope`, `--domain`, `--body`) |
| `link <ref> --depends-on <ref2>` | Add edge(s) to a doc |
| `unlink <ref> --depends-on <ref2>` | Remove edge(s) from a doc; accepts literal 14-digit dead ids as edge targets for removal (doc refs still resolve normally) |
| `history <ref> --add "<entry>"` | Append a history entry to a doc |
| `rm <ref> [--force] [--dry-run]` | Delete a doc; blocked when **any inbound edge** exists unless `--force`; `--force` strips **all inbound edges** then deletes; reindexes automatically |
| `ingest-raw` | Write verbatim content into the raw/ tier (outside the graph) |
| `inbox add/list`, `promote` | Inbox pipeline gates 0 and 1 |

### Review ledger

| Verb | What it does |
|------|--------------|
| `review new --since <ISO>` | Create a post-hoc review summary for an editing episode |
| `review list [--unsigned-by <who>]` | List review records |
| `review show <ref>` | Show a review record |
| `review sign <ref> [--as <who>]` | Sign a review; default `who` from `ldoc config` user identity |

### User config

| Verb | What it does |
|------|--------------|
| `config user.name [<value>]` | Get/set the configured display name (git-style) |
| `config user.email [<value>]` | Get/set the configured email |
| `config signer` | Print the composite `Name <email>` sign-off string |
| `config --list` | List user preference keys |
| `config --bootstrap-from-git` | Copy user.name/email from `git config --global` if unset |

`ldoc config` does **not** require a located store. See user-config-identity.

### Maintenance

| Verb | What it does |
|------|--------------|
| `validate` | Structural integrity checks across all docs. Also warns on: staged-incomplete retirement (`superseded_by` present but `status` ≠ `deprecated`); body `[[id]]` wikilinks not present in any edge field (report only — no auto-edges); malformed body wikilinks (`[[id\|label]]`, `[[id]] (label)` — bodies must use bare `[[id]]`). Dangling outbound edge detection is unified with `ldoc edges`. |
| `reindex` | Rebuild `docs/.index/` artifacts (hierarchy, dependents, orphans) |
| `viewer [--out PATH]` | Build the read-only HTML viewer (also runs automatically after mutating commands — see below) |

**Auto viewer rebuild:** after any successful command that mutates **docs/** or **reviews/** (doc mutators and `review new` / `review sign`), `ldoc` rebuilds `STORE_ROOT/build/viewer.html` silently by default (see `[[20260702205006]]`). Disable per invocation with `--no-viewer`, or globally with `LIVEDOCS_AUTO_VIEWER=0`. Set `LIVEDOCS_AUTO_VIEWER_VERBOSE=1` for a one-line stderr notice. Rebuild failures are swallowed — porcelain must not fail because the viewer is stale. See `[[20260623191806]]` (Auto Viewer Rebuild).

### Ref resolution

Every `<ref>` argument accepts: canonical id (e.g. `20260615230652`), label (e.g. `porcelain-api`), or title substring (case-insensitive, unambiguous match required). Multi-word labels are quoted on the CLI. Pass `-` as the sole ref to read refs from stdin.

---

> **Knowledge vs implementation boundary:** this doc describes the *capability* of `ldoc`. The implementation lives in `scripts/livedocs/` and `scripts/ldoc.py` and is kept in sync by humans and the panel — live_docs does not govern its own tooling.
