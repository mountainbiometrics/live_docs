---
id: "20260624204553"
title: "Decision: Viewer displays schema enums in Title Case"
label: "Viewer Enum Title Case"
summary: "In the viewer, type, status, and level values render Title Case (Use Case, Living, Preference). Scope and domain values stay store-defined. Filtering and sort still use raw enum strings."
type: decision
status: living
level: preference
belongs_to: ["[[20260619204727]]"]
provenance: ["[[20260624204541]]"]
domain: [presentation]
created: "2026-06-24T20:45:52Z"
---

## Context

Schema enums (`type`, `status`, `level`) are stored as lowercase kebab/snake strings (`use-case`, `living`, `preference`). Scope and domain values are an open, store-defined vocabulary with their own conventions. The viewer displays frontmatter widely — meta lines, tooltips, dashboard stats, catalog cells, dropdown labels — and raw enum strings read as implementation detail.

## Decision

The viewer **displays** `type`, `status`, and `level` in **Title Case** everywhere they appear (`use-case` → "Use Case", `living` → "Living"). **Scope and domain** values render **as stored** — they are governed vocabulary from the KB, not fixed schema enums.

Display formatting is presentation-only: filters, sort keys, and preset logic continue to use the raw frontmatter strings.

## Consequences

- Implemented client-side via a shared `enumDisplay` helper in `scripts/viewer/template.html`.
- Catalog click-to-filter chips carry raw values in data attributes; only the visible label is Title Case.
