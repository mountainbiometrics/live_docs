---
id: "20260615182358"
title: "live_docs captures knowledge, not implementation governance"
label: "Knowledge Not Implementation"
summary: "live_docs captures and keeps current the knowledge within an implementation; it does not govern the implementation itself. Skills and scripts are code, not graph nodes. A component doc about a skill is knowledge, not a governance hook — code-to-knowledge sync remains deferred to the panel layer."
type: decision
status: living
level: preference
belongs_to: ["[[20260618230226]]"]
requires: ["[[20260615100009]]", "[[20260615100018]]"]
created: "2026-06-15T18:23:58Z"
history:
  - at: "2026-06-22T17:59:53Z"
    summary: "garden consistency: linked belongs_to 20260618230226 (Store Layout and Config) — orphan pre-curate-grouping"
  - at: "2026-06-24T19:58:53Z"
    summary: "garden-refine: label cleanup — 'Knowledge Not Impl' → 'Knowledge Not Implementation'"
---

## Context

While bootstrapping, the cascade dogfood test observed that the operational skills (`cascade-check`, `garden`, etc.) live in `.claude/skills/` and are not docs in the graph — so a decision that governs a skill's behavior (e.g. the cascade verdict vocabulary) has no edge into the skill's code. This raised the question of whether live_docs should "self-govern" its own implementation. That question conflates two distinct concepts: *implementing* the live_docs system vs. *documenting the knowledge within* an implementation.

## Decision

live_docs captures and keeps current the **knowledge** within an implementation; it does not govern the implementation itself. Skills and scripts are implementation (code). Documentation *about* a skill is a `component` knowledge doc — a description, not a governance hook. Keeping such a component doc in sync when the underlying implementation changes is the panelists' job (the future code↔knowledge sensor layer), or is done today by re-ingesting. A governing decision therefore has no cascade edge into skill code, and that is correct.

## Consequences

- The "skills live outside the graph" observation is a **boundary** of the system, not a defect; no self-governance machinery is needed.
- `component` docs may be created for skills, but they document knowledge; they do not auto-update code, nor does code auto-update them within live_docs alone.
- Code↔knowledge synchronization remains deferred to the panel layer (see the doc↔doc-only and panel-integration decisions this depends on).
