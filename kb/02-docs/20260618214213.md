---
id: "20260618214213"
title: "The store is navigable as a visual graph in Obsidian"
label: "Navigable Graph"
summary: "A first-class way to explore the store is the Obsidian graph view: nodes should read as {type}: {label} rather than as opaque ids, the distinct edge types should be visually distinguishable, and a doc's summary should be reachable on hover. The rationale is that the store is intentionally atomic and id-opaque, which makes flat browsing hard, so a typed, labeled graph restores human navigability. Config-only wins toward this goal are applied directly; richer needs are met by read-only plugins, consistent with the one-schema-authority constraint that external tools never write back."
type: goal
status: living
level: preference
relates: ["[[20260615100004]]", "[[20260615100010]]", "[[20260618214152]]"]
provenance: ["[[20260618214113]]"]
scope: [live_docs]
created: "2026-06-18T21:42:13Z"
---

## Statement

A first-class way to explore the store is the **Obsidian graph view**. To make it useful:

- nodes read as `{type}: {label}` (not opaque ids),
- the distinct edge types are visually distinguishable,
- a doc's summary is reachable on hover.

## Rationale

The store is intentionally **atomic and id-opaque**, which makes flat directory browsing hard. A typed, labeled graph restores human navigability — a reader can see clusters, follow edge types, and read summaries without opening files.

## How the goal is met

Config-only wins (graph display settings, node labels) are applied directly. Richer needs are met by **read-only** plugins only — write-back plugins are excluded by the one-schema-authority constraint, so the graph layer enhances navigation without ever owning the data.
