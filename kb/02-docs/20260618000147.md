---
id: "20260618000147"
title: "apply-to-docs — skill that materializes a request or plan into the store"
label: "Apply To Docs Skill"
summary: "Accepts a user request or plan, extracts the concepts it asserts, surveys the store, and applies all writes in a single coherent batch. A strict two-pass structure (read-only identify, then batch write) prevents the sequential-patch problem. A pause gate fires for unresolved conflicts or unintended side-effects — not merely for a large expected impact set."
type: component
status: living
level: preference
belongs_to: ["[[20260618230224]]"]
requires: ["[[20260618000251]]"]
relates: ["[[20260616181843]]", "[[20260623233949]]"]
domain: [agent-workflows]
history:
  - at: "2026-06-18T00:01:47Z"
    summary: "Created doc"
    change_type: [addition]
  - at: "2026-06-18T00:16:56Z"
    summary: "apply-to-docs: revised — added two-pass structure section explaining the sequential-patch problem and why read-only survey + batch write prevents it; expanded processing phases to include the graph-expansion step (Step 4)"
  - at: "2026-06-18T23:03:14Z"
    summary: "curate-grouping: grouped under index 20260618230224 (Ingest & Inbox Pipeline)"
  - at: "2026-06-23T23:40:38Z"
    summary: "synthesize-doc-changes: revised — Write phase now applies the shared belongs_to-placement policy so each newly created doc is born with a parent instead of as an orphan. Related to 20260623233949."
  - at: "2026-06-24T19:58:52Z"
    summary: "garden-refine: label cleanup — 'Apply-to-docs Skill That' → 'Apply To Docs Skill'"
  - at: "2026-06-24T20:08:36Z"
    summary: "garden-domains: assigned domains agent-workflows"
  - at: "2026-07-01T19:51:20Z"
    summary: "revise-doc: dropped the fixed 3–10 extraction count (extract however many durable concepts the request genuinely asserts) and the 'concept claim' phrasing (collapsed to 'concepts' — the Concept/Claim label distinction was meaningless indirection). Per user simplification request 2026-07-01."
  - at: "2026-07-23T22:42:41Z"
    summary: "revise-doc: gate is conflicts/side-effects not size — align with pause requirement 2026-07-23"
    change_type: [revision]
    session: "20260723224229-b9d8"
---

## Component

The `apply-to-docs` skill accepts a user request or plan in any form (inline text, file path, structured proposal), extracts the key concepts it asserts, surveys the live_docs store for docs whose claims match or conflict with those concepts, and applies all writes in a single coherent pass.

## Two-pass structure

The skill separates work into two strictly ordered passes:

1. **Identify (read-only)** — survey every doc the request touches, walk the dependency graph for cascade neighbors, and build a complete impact set. No writes occur during this pass.
2. **Synthesize (write)** — apply all changes in one coherent batch, with the full picture in view.

This ordering prevents the sequential-patch problem: if writes are applied one at a time, each write is informed only by prior writes rather than the complete final state. The read-only pass + batch-write structure guarantees that every doc in the impact set is revised with knowledge of all other revisions simultaneously.

## Processing phases

1. **Normalize** — restate what the request intends to be true afterward.
2. **Extract** — identify the durable concepts it asserts; type each as principle/decision/constraint/requirement/use-case/component.
3. **Survey** — search the store for each concept; classify each matching existing doc as compatible, partial-supersession, or full-supersession.
4. **Expand** — walk the dependency graph (BFS with visited set) from every directly-matched doc; assess cascade neighbors; build the complete impact set. Entire step is read-only.
5. **Gate** — pause only for unresolved conflicts or judged unintended side-effects (impact outside what the request implies). Do **not** pause solely because the expected impact set is large; the survey still always runs to inform synthesis.
6. **Write** — apply in order: deprecations first, revisions second, new docs third. Within each category, work upstream to downstream. Write each doc as its single correct current state, not as a sequential patch. When the write phase creates a doc, it also **places it in the hierarchy at birth** — applying the shared belongs_to-placement policy to give the new doc a semi-adequate parent, so it is born in the tree rather than dumped as an orphan for gardening to home later.
7. **Validate** — run `ldoc validate`; address errors before finishing.
8. **Review** — emit a review summary for the episode.
