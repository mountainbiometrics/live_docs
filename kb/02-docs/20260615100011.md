---
id: "20260615100011"
title: "Two adoption axes: level and state"
label: "Adoption Axes"
summary: "Deprecated. Originally split doc adoption metadata into two fields: level (settledness) and state (temporal target: actual or target). Superseded by the June 2026 schema migration that absorbed state into the status enum — status: living replaces state: actual, status: target replaces state: target."
type: decision
status: deprecated
level: preference
requires: ["[[20260615100001]]", "[[20260615100002]]"]
provenance: ["[[20260615203646]]"]
superseded_by: ["[[20260617212538]]"]
scope: [live_docs]
created: "2026-06-15T10:00:11Z"
history:
  - at: "2026-06-17T21:26:49Z"
    summary: "deprecated: state field removed in schema migration; state:actual->status:living, state:target->status:target; superseded by 20260617212538 (status enum decision)"
---

## Context

Docs need metadata expressing how settled or authoritative they are. A single "adoption" field was considered, but it conflates two orthogonal dimensions.

## Decision

Adoption is split into two fields: `level` (provenance/settledness: incidental → trial → preference → requirement) and `state` (temporal target: actual | target). These are independent axes and must remain separate fields.

## Consequences

A doc can be `level: requirement, state: target` (this is what we want but haven't achieved yet) or `level: incidental, state: actual` (this is happening but wasn't a deliberate decision). Collapsing them into one field would make these combinations inexpressible or require awkward compound values. The separation enables precise drift detection: a `state: target` doc that hasn't become `actual` after some time is a trackable gap (P2, P3).

## Correction

The `state` field no longer exists. The schema migration (2026-06-17) absorbed `state: actual|target` into the `status` enum: `status: living` replaces `state: actual`, and `status: target` replaces `state: target`. The `level` field is unchanged. The intent — distinguishing realized from intended — is now expressed by the four-value `status` enum. See [[20260617212538]].
