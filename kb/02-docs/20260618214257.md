---
id: "20260618214257"
title: "Box paths are configurable; scripts read paths from config, skills stay path-agnostic"
label: "Configurable Boxes"
summary: "Directory locations for the inbox, raw, docs, reviews, and index boxes are resolved by precedence: environment variable, then livedocs.config.json, then a built-in default. Scripts never hardcode paths and skills never reference paths at all — skills go entirely through ldoc. The rationale is that a deployment must be able to relocate its boxes without editing code, which the multi-repo store will require, and the restructure to a kb/ parent must not ripple into the skills layer."
type: decision
status: living
level: preference
requires: ["[[20260615185417]]"]
relates: ["[[20260618214227]]"]
provenance: ["[[20260618214113]]"]
scope: [live_docs]
created: "2026-06-18T21:42:57Z"
---

## Decision

Box directory locations (inbox / raw / docs / reviews / index) are resolved by **precedence**: environment variable > `livedocs.config.json` > built-in default. **Scripts never hardcode paths**; **skills never reference paths at all** — they go through `ldoc`.

## Rationale

A deployment must be able to **relocate its boxes** without editing code — the multi-repo store will need this. Routing all path resolution through config (and all skill access through `ldoc`) means the `kb/` restructure, and any future relocation, ripples no further than a config value. This extends the tooling-logic-in-scripts decision to path resolution specifically.
