---
id: "20260624201944"
title: "Clipping: viewer domain pills, condensed scope path, catalog domain filters"
type: reference
kind: clipping
status: reference
original_source: "user-request"
imported: "2026-06-24"
---

The viewer (the html template) doesn't currently do anything with "domains", but these should be just as prominent as the "scope" (should be displayed as pills just like the scope in all the headers and stuff); and on the catalog "page" specifically it should have domain filters (that table doesn't need the 'title', that's redundant with the 'label' in that view specifically).
Also: scope could be condensed... So your current scope is potentially a lot of things, but that's in a hierarchy; so (in our kb for example) a doc under 'viewer' also gets the scope `live_docs`, so you legitimately have both; but display-wise this could be a single pill with `live_docs/viewer`.
Can you help me make these improvements (and record this with /apply-to-docs )
