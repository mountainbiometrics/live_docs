---
id: "20260615100006"
title: "Cascade checks are agent-invoked skills, not git hooks/CI"
label: "Agent-Invoked Cascade"
summary: "Cascade checks — which propagate the effects of a changed doc to its neighbors — are implemented as an on-demand agent skill rather than wired into git hooks or CI pipelines. This keeps the system tool-agnostic: it works identically with or without git or a CI environment present. The tradeoff is that cascade checks are not automatic on every commit; silent drift from missed invocations is addressed through periodic gardening passes rather than hard automation."
type: decision
status: living
level: preference
requires: ["[[20260615100000]]", "[[20260615100005]]"]
provenance: ["[[20260615203646]]"]
scope: [live_docs]
created: "2026-06-15T10:00:06Z"
---

## Context

Cascade checks need to run when a doc changes. The obvious integration points are git hooks or CI pipelines, but both require specific tooling to be configured and present.

## Decision

Cascade checks are implemented as a `cascade-check` skill invoked on demand by an agent, not wired into git hooks or CI.

## Consequences

The system remains tool-agnostic (P1): it works identically with or without git. Checks are not automatic on commit, but that is acceptable — the skill can be called by any agent or human workflow that knows a doc changed. The tradeoff is that a user who never invokes the skill can accumulate silent drift; this is addressed by gardening pass skills (P6).
