---
id: "20260616200133"
title: "References render as typed wiki-links"
label: "Typed Wiki-Link Refs"
type: decision
status: living
level: preference
state: target
depends_on: [20260615100004]
references: [20260615230652]
tags:
  domain: []
  scope: [live_docs]
created: "2026-06-16T20:01:33Z"
history: []
---

## Context

Docs reference other docs via depends-on and references edges. Human-facing output currently shows labels without type context, making a bare name ambiguous when the type (Principle / Decision / Component / Goal / Reference) is not known from context.

## Decision

Any doc reference rendered to a human is formatted as a **typed wiki-link**:

```
[<Type>: <Title>](<id>.md)
```

- `<Type>` is the capitalized doc type (Principle, Decision, Component, Goal, Reference).
- `<Title>` is the full doc title.
- `<id>.md` is the canonical filename, giving a navigable link in any markdown viewer.
- The format is readable raw (type and title are human-legible), carries the id for programmatic navigation, and makes the type always present so a bare label is never ambiguous.

### Deferred sub-choice

Whether the link text uses the full **Title** or the shorter **label** is left to the label convention (see label-and-title). The current rendering uses Title; a future revision may substitute the label when it is sufficiently descriptive and more compact.

## Consequences

- No reference shown to a human is a bare id or a bare label without type context.
- Agents and humans can immediately tell what kind of doc is being referenced without an extra `ldoc label` call.
- JSON output is unaffected — it remains id-keyed for machine consumers.
- The review-summary ledger already renders entries with `<Type>: <label> — <intent>`; this decision formalizes the pattern for all reference output.
