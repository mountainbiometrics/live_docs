---
id: "20260615100002"
title: "Dependency Tracking & Enforcement"
label: "Dependency Tracking"
summary: "Every doc in the live_docs store explicitly records what it depends on, turning invisible drift vectors into checkable relationships. When doc A depends on doc B, that relationship is stored so that a change to B triggers a cascade check on A. Without this tracking, a downstream doc can silently become stale because no one knew it was influenced by the changed source. Making dependencies explicit is the foundational mechanism that lets the system detect and surface drift rather than let it accumulate."
type: principle
status: living
level: requirement
provenance: ["[[20260615203646]]"]
scope: [live_docs]
created: "2026-06-15T10:00:02Z"
---

## Statement

Systems and code have dependencies; documentation must convey and enforce that same complexity. Every doc tracks what influences it, and changes are checked against the dependency graph.

## Rationale

Undocumented dependencies are invisible drift vectors. When doc A changes because it depends on doc B, and that relationship is not recorded, B can be updated without anyone knowing A is now stale. Making dependencies explicit turns an invisible problem into a checkable one.
