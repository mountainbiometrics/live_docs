# Ledger — ingest-reference

Edit history for skill refinements. Newest first.

## 2026-08-04 — Drop ambiguity→target bias; use shared status definition (Class B)

**Failure:** Suite stamped current/in-force concepts `status: target`. This
skill's status-inference block said "prefer `target` when ambiguous," fighting
the corrected default.

**Class:** B — quoted instruction permitted the bad reading. Prior bias against
plan-tone→`living` wholesale is preserved via per-concept classification, not
an ambiguity default to `target`.

**Diff:** Status inference → pointer to `_shared/status-living-vs-target.md`;
checklist updated. Deliberate scope change vs old ambiguity→`target` bias
(user-approved in refining-skills session).

**Regression answers (reconstructed anchors):**
1. Gate 1.5 shard then decompose — unchanged.
2. Conflict scan before writes — unchanged.
3. Provenance to NORM_ID — unchanged.
4. Bodies claim/why not implementation narration — yes; status via shared file.
5. One episode review — unchanged.
