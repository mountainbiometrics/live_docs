# Regression notes — ingest-reference

Anchors for refinement. Reconstructed 2026-08-04 from skill text; replace with
cited happy-path outputs when available.

## Known-good properties (must keep permitting)

1. **Shard then decompose** — gate 1.5 volume check before comprehension.
2. **Conflict scan before writes** — map-concepts before synthesize.
3. **Provenance to NORM_ID** — extracted docs link the normalized reference.
4. **Bodies state claim and why** — not implementation history/absence.
5. **One episode review** owned by this orchestrator.

## Newly discovered good-output properties (2026-08-04)

6. **Per-concept status via shared deferral test.** Default `living`;
   ambiguity → `living`. `target` only under
   `_shared/status-living-vs-target.md`. Do not stamp a whole plan one status
   from document tone.
